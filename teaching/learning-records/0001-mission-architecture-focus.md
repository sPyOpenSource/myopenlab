# Mission shifted from GUI-building to module architecture

The mission changed from "Build GUI/Dashboard Apps with MyOpenLab" (old MISSION.md) to "Understand MyOpenLab's module architecture" — how the system loads and runs elements, so the user can navigate the codebase with confidence. [[MISSION.md]]

**Evidence:** User stated the goal directly in a Q&A (2026-07-31); confirmed the mission update explicitly.

**Implications:** Future lessons teach architecture first (module anatomy, loading, event-driven execution) rather than GUI-building. Lesson 0001 (VM basics) remains a valid foundation; the course trajectory pivots after it. Building new elements becomes a *follow-up* mission, not the current one.
